image:
  registry: ghcr.io
  repository: tourmalinecore/pelican-env-synchronizer
  pullPolicy: Always

resources:
  limits:
    # WARNING: small value of cpu limit may cause failing during applying helmfile
    cpu: 500m
  requests:
    cpu: 1m
    memory: 256Mi

extraConfigMapEnvVars:
